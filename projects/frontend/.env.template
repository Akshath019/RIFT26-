# GenMark Frontend — Environment Variables
# ==========================================
# Copy this file to .env and fill in your values.

# ── Backend URL ────────────────────────────────────────────────────────────────
# URL of the deployed GenMark FastAPI backend (Render free tier)
# For local development: http://localhost:8000
# For production: https://genmark-backend.onrender.com
VITE_BACKEND_URL=http://localhost:8000

# ── Algorand Network (optional — kept for compatibility with base template) ────
# The frontend does NOT call Algorand directly — all blockchain interactions
# go through the backend. These variables are only used if you integrate
# wallet features for advanced use cases.

# TestNet configuration (recommended)
VITE_ALGOD_TOKEN=""
VITE_ALGOD_SERVER=https://testnet-api.algonode.cloud
VITE_ALGOD_PORT=""
VITE_ALGOD_NETWORK=testnet
VITE_INDEXER_TOKEN=""
VITE_INDEXER_SERVER=https://testnet-idx.algonode.cloud
VITE_INDEXER_PORT=""
